{
  "name": "exercise-sequelizer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "migrate":"npx sequelize-cli db:migrate",
    "make-model":"sequelize model:generate --name user --attributes name:string,email:string,role:string",
    "down":"npx sequelize-cli db:migrate:undo",
    "seed":"npx sequelize-cli db:seed:all",
    "make-seed":"npx sequelize-cli seed:generate --name demo-user",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.7.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.17.0",
    "sequelize-cli": "^6.4.1"
  }
}
